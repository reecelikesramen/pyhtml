{
    "name": "pyhtml-client",
    "version": "0.0.1",
    "description": "PyHTML client-side library with transport fallbacks",
    "private": true,
    "scripts": {
        "build": "node build.mjs",
        "build:dev": "node build.mjs --dev",
        "watch": "node build.mjs --watch",
        "test": "vitest run",
        "test:watch": "vitest",
        "test:coverage": "vitest run --coverage"
    },
    "devDependencies": {
        "@types/morphdom": "^2.3.0",
        "@vitest/coverage-v8": "^4.0.18",
        "esbuild": "^0.27.2",
        "jsdom": "^24.0.0",
        "typescript": "^5.3.0",
        "vitest": "^4.0.18"
    },
    "dependencies": {
        "@msgpack/msgpack": "^3.1.3",
        "morphdom": "^2.7.7"
    }
}